import './Config.scss'

import Card from "@/components/Card/Card"

const Config = ({ onClose }) => {

    return (
        <Card className="config config-card">
            <div className="config-header">
                Config and Reset
            </div>
        </Card>
    )
}

export default Config
